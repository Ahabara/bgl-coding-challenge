import React from 'react';
import '../App.css';

const Title = () => {
    return (
        <div>
            <h1 className="title">Profit and Loss Statement</h1>
        </div>
    );
};

export default Title;